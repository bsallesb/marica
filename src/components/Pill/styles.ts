import { Link } from 'react-router-dom';
import styled from 'styled-components';

export const PillBtn = styled(Link)`
    border-radius: 50px;
    padding: 2px 20px 2px 20px;
    white-space: nowrap;
`;

export const PillExternalBtn = styled.a`
    border-radius: 50px;
    padding: 2px 20px 2px 20px;
    white-space: nowrap;
`;
